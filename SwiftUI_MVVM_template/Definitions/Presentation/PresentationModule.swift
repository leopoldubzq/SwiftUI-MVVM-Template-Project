enum Home {
    typealias ViewModel = BaseViewModel & HomeViewModelProtocol
}
enum Profile {
    typealias ViewModel = BaseViewModel & ProfileViewModelProtocol
}
enum ArticleDetails {
    typealias ViewModel = BaseViewModel & ArticleDetailsViewModelProtocol
}
